# PR #36 Review Ledger

## Target and evidence inventory

- Repository: `narumiruna/pi-vscode`; checkout and `origin` match the PR's head repository. Target: [PR #36](https://github.com/narumiruna/pi-vscode/pull/36), branch `narumi/feat/semantic-context-review-diagnostics`.
- Reviewed head: `9e0a3a8b7bba71869b5209ed028596fdd5a2ee70`; base: `e4a381696cfb29110b3d84aa01a6eff57233b7e3`. The review refers to `77ae3b3`; the later commit only records completion and removes the plan, so none of its three concerns is superseded.
- Fetched the description, all four commits, complete 104-file diff, all paginated file/review/comment lists, and all three GraphQL threads. Each thread has one comment and no further comment page. The same implementation/formatting diff was reviewed during the original work; feedback verification below independently checks the affected source and failure paths.
- One submitted review contains three findings. Its remaining text and the single conversation comment are automated review-status/usage information, not additional product feedback or instructions to execute.
- No check runs or commit statuses are published. GitHub's combined status is `pending` with an empty status list, not evidence of a failing check. Validation must be reported from actual local runs.
- No applicable `AGENTS.md` was found. Existing VS Code engine (`^1.106.0`), workspace host placement, trust guards and release boundaries remain unchanged.

## Feedback ledger

| ID | Feedback | Independent evidence and scope | Severity | Outcome |
| --- | --- | --- | --- | --- |
| R1 | [4058892846](https://github.com/narumiruna/pi-vscode/pull/36#discussion_r4058892846): classify `vscode_code_context` as read-only for checkpoints | The new packaged tool and read-only gate recognize semantic lookup, but `WorkspaceChangeTracker.captureToolEvent` omits it from known tools, sets `unprovenEffects`, and `finishRequest` excludes otherwise attributable writes. The PR introduces the tool, exposing this integration defect; preserving existing checkpoint behavior is in scope. | P2: loss of preview/revert coverage, not an unsafe automatic write | Already addressed in this review pass. The exact tool name is allowed; the real subprocess-write fixture now proves semantic lookup preserves Preview/Revert and restores original bytes. Unknown tools and shell effects still fail closed. |
| R2 | [4058892848](https://github.com/narumiruna/pi-vscode/pull/36#discussion_r4058892848): derive semantic paths from the workspace URI | Source, provider-target and excerpt checks all subtract canonical `realpath(folder)` from the editor's noncanonical URI. A workspace root alias therefore produces `../alias/...`, which `assertSafeFile` rejects. This affects both newly added semantic entry points. Workspace-root aliases must not permit nested file/directory symlinks or foreign results. | P2: semantic workflow unavailable in a valid file-backed workspace | Already addressed in this review pass. All three relative-path calculations use the workspace URI; normal/aliased real-filesystem fixtures cover targets, excerpts, authenticated bridge requests, and nested file/directory symlink rejection. Native aliased-workspace UI passes on both supported test versions. |
| R3 | [4058892851](https://github.com/narumiruna/pi-vscode/pull/36#discussion_r4058892851): retain diagnostics in symlinked workspaces | Collection derives names from canonical `root` rather than the selected folder URI and silently skips aliased diagnostics. Changing collection alone is insufficient: the new multi-file adapter also compares aliased document URIs to canonical `snapshot.root`. Complete repair requires binding editor URI spelling to the captured canonical root while retaining canonical operation locking and rejecting root retargeting. All affected code is introduced by this PR. | P2: batch repair unavailable; fix must preserve mutation safety | Already addressed in this review pass. Capture preserves the editor workspace spelling; the adapter separately owns the canonical root lock and validates alias identity before Preview/Apply and after asynchronous preflight. Two-file workflow, retargeted-root, nested-leaf-symlink, stale, failed-Apply and disposal regressions pass. |

## Validation and publication

- Against unchanged production code at `9e0a3a8`, the new/extended fixtures fail four assertions: R1 returns zero restorable files instead of one; R2 rejects the alias source as an unsafe relative path and its authenticated bridge returns `ok: false`; R3 submits zero repair requests instead of one. The other ten focused assertions/tests pass. Command: `npm run compile && npx vitest run out/test/checkpointHistory.test.js out/test/semanticContext.test.js out/test/workspaceDiagnosticsController.test.js`.
- After completing this ledger, refreshed PR state: **OPEN**, unmerged, same head. Implement fixes on its existing branch; no follow-up PR is needed unless the target merges during this work.
- Changing only the R3 collector reproduced the second failure: `Edit target is outside the selected workspace.` The adapter fix is therefore required to complete the original diagnostic-repair goal, not a separate enhancement.
- Focused validation: 15 tests across checkpoint, semantic, diagnostic-controller and multi-file adapter files pass. Full validation: **217 tests / 41 files** pass normally, with `GIT_INDEX_FILE=.git/index`, and during packaging. Whole-repository Biome passes with zero errors (380 warnings / 9 infos); production audit reports zero vulnerabilities; `git diff --check` passes.
- Native Linux VS Code **1.106.0 and 1.138.0** UI runs pass in both ordinary and directory-alias workspaces. The runner asserts VS Code retains the alias URI spelling, then exercises semantic attachments, all four reviews, Problems Ask/Fix, two-file selected-hunk Preview/Apply, one Undo, cancellation and reload. Separate Restricted Mode launches pass. These use deterministic RPC responses, not a live provider; Windows/SSH alias workflows were not rerun or newly claimed.
- The real Pi registration probe still confirms foreground-only tool registration. The inspected VSIX has 63 entries / 50 runtime modules, includes the fixes, excludes source/tests/docs/scripts/maps/development dependencies, and leaves the original ignored VSIX unchanged. SHA-256: `0603bf82cbe7641840f2566263631676004a1c3ecaaeb2a64a7255b2b191f22c`.
- Final fix review covers URI ownership, canonical locking, retained nested-symlink guards, alias retargeting before and during asynchronous Apply preflight, unchanged public commands/engine/host, lifecycle disposal and scope. No deferred item, clarification request or blocker remains. Published commit and resolution evidence are reported in the linked PR review replies.
