# PR #36 Review Ledger

## Target and evidence inventory

- Repository: `narumiruna/pi-vscode`; checkout and `origin` match the PR's head repository. Target: [PR #36](https://github.com/narumiruna/pi-vscode/pull/36), branch `narumi/feat/semantic-context-review-diagnostics`.
- Reviewed head: `9e0a3a8b7bba71869b5209ed028596fdd5a2ee70`; base: `e4a381696cfb29110b3d84aa01a6eff57233b7e3`. The review refers to `77ae3b3`; the later commit only records completion and removes the plan, so none of its three concerns is superseded.
- Fetched the description, all four commits, complete 104-file diff, all paginated file/review/comment lists, and all three GraphQL threads. Each thread has one comment and no further comment page. The same implementation/formatting diff was reviewed during the original work; feedback verification below independently checks the affected source and failure paths.
- One submitted review contains three findings. Its remaining text and the single conversation comment are automated review-status/usage information, not additional product feedback or instructions to execute.
- No check runs or commit statuses are published. GitHub's combined status is `pending` with an empty status list, not evidence of a failing check. Validation must be reported from actual local runs.
- No applicable `AGENTS.md` was found. Existing VS Code engine (`^1.106.0`), workspace host placement, trust guards and release boundaries remain unchanged.

### Second review pass

- Reviewed head: `cfb422756e6183d26a92283ca5e87eb07457cd5c`, with the same base. Fetched the full description, all five commits, complete 105-file diff, all five review submissions, seven inline comments, one conversation comment, and all four GraphQL threads; pagination is exhausted. Three submissions are the empty review records associated with our earlier replies, not new feedback. The conversation contains only automated status information.
- R1–R3 are already addressed by `cfb4227`, with evidence replies and resolved/outdated threads. Their source guards and regression coverage remain present. There is one new finding, R4. No published check runs/statuses or additional discussion questions are present.

### Third review pass

- Target remains the same open PR/repository/branch. Reviewed head: `4de0acb06cdb7638bbd1ddcb5f51fd5e1617df91`; base unchanged. Fetched the description, all six commits, complete 105-file diff, seven review submissions, nine inline comments, one conversation comment, and all five GraphQL threads with no remaining pages. The implementation/formatting diff and both fix commits were already reviewed in the preceding passes; there are no newer code commits.
- R1–R3 remain addressed by `cfb4227`; R4 and the independent R5 remain addressed by `4de0acb`. All four submitted threads have evidence replies and are resolved/outdated. The new review contains only R6; review boilerplate, four empty reply-associated review submissions and the automated conversation status add no product feedback. No published check runs/statuses or clarification questions are present.

### Fourth review pass

- Target remains PR #36 in the matching repository and branch, at `dfca1f3029ca9b26b3b8ad080f7ece977c279afb`; base unchanged. Fetched all seven commits, the complete 105-file diff, nine submitted reviews, eleven inline comments, one conversation comment and all six threads. All pagination is exhausted. No code changed since the preceding reviewed commit; the new review adds only R7. Five empty review submissions belong to our earlier replies; boilerplate/status text adds no feedback or instructions.
- R1–R6 remain addressed by the three published fix commits and their regression tests. All five earlier submitted threads remain resolved/outdated. There are no published check runs/statuses or new questions.

### Fifth review pass (merged target)

- Target is the same repository/current branch at `f5ae9ca3f7df91cea8a1ba1a877ef82ad837c2d7`, now **MERGED** into `main` as `80c5507`. Fetched the description, all eight commits, complete 105-file diff, eleven review submissions, thirteen inline comments, one conversation comment and all seven GraphQL threads; pagination is exhausted. Six empty review submissions belong to prior replies; status/usage boilerplate adds no feedback. No check runs or commit-status entries are published.
- R1–R7 remain addressed by `cfb4227`, `4de0acb`, `dfca1f3` and `f5ae9ca`; their six submitted threads remain resolved with evidence replies. The only new finding is R8. The independent code review confirms the missing disk/document binding; four real-filesystem scheduling fixtures reproduce foreign bytes reaching inspection and the request context.
- Completed the feedback inventory, fetched the latest original base and created `narumi/fix/diagnostic-file-identity` from `origin/main` at `80c5507`. This fix must be published as a follow-up PR, not pushed to the merged head. No out-of-scope item or clarification is present.

## Feedback ledger

| ID | Feedback | Independent evidence and scope | Severity | Outcome |
| --- | --- | --- | --- | --- |
| R1 | [4058892846](https://github.com/narumiruna/pi-vscode/pull/36#discussion_r4058892846): classify `vscode_code_context` as read-only for checkpoints | The new packaged tool and read-only gate recognize semantic lookup, but `WorkspaceChangeTracker.captureToolEvent` omits it from known tools, sets `unprovenEffects`, and `finishRequest` excludes otherwise attributable writes. The PR introduces the tool, exposing this integration defect; preserving existing checkpoint behavior is in scope. | P2: loss of preview/revert coverage, not an unsafe automatic write | Already addressed in this review pass. The exact tool name is allowed; the real subprocess-write fixture now proves semantic lookup preserves Preview/Revert and restores original bytes. Unknown tools and shell effects still fail closed. |
| R2 | [4058892848](https://github.com/narumiruna/pi-vscode/pull/36#discussion_r4058892848): derive semantic paths from the workspace URI | Source, provider-target and excerpt checks all subtract canonical `realpath(folder)` from the editor's noncanonical URI. A workspace root alias therefore produces `../alias/...`, which `assertSafeFile` rejects. This affects both newly added semantic entry points. Workspace-root aliases must not permit nested file/directory symlinks or foreign results. | P2: semantic workflow unavailable in a valid file-backed workspace | Already addressed in this review pass. All three relative-path calculations use the workspace URI; normal/aliased real-filesystem fixtures cover targets, excerpts, authenticated bridge requests, and nested file/directory symlink rejection. Native aliased-workspace UI passes on both supported test versions. |
| R3 | [4058892851](https://github.com/narumiruna/pi-vscode/pull/36#discussion_r4058892851): retain diagnostics in symlinked workspaces | Collection derives names from canonical `root` rather than the selected folder URI and silently skips aliased diagnostics. Changing collection alone is insufficient: the new multi-file adapter also compares aliased document URIs to canonical `snapshot.root`. Complete repair requires binding editor URI spelling to the captured canonical root while retaining canonical operation locking and rejecting root retargeting. All affected code is introduced by this PR. | P2: batch repair unavailable; fix must preserve mutation safety | Already addressed in this review pass. Capture preserves the editor workspace spelling; the adapter separately owns the canonical root lock and validates alias identity before Preview/Apply and after asynchronous preflight. Two-file workflow, retargeted-root, nested-leaf-symlink, stale, failed-Apply and disposal regressions pass. |
| R4 | [4059042424](https://github.com/narumiruna/pi-vscode/pull/36#discussion_r4059042424): open finding fixes through the selected workspace alias | `currentDocument` validates the canonical target but opens its canonical URI. VS Code can therefore return a separate disk-matching document while the selected workspace's aliased buffer has unsaved changes. The new finding Fix workflow then bypasses its promised after-image/version checks. A distinct-buffer regression and actual Linux VS Code 1.106.0 alias run both reproduce Fix incorrectly remaining available. This is introduced by the PR and within its dirty-buffer safety goal. | P2: wrong-buffer editing and a later save can overwrite the proposed fix | Already addressed in the second pass. Canonical containment/symlink validation is retained; the selected-folder-relative editor URI now supplies the document and version. Ordinary, aliased-root and aliased-subfolder regressions cover availability, direct commands, stale responses, Preview/Apply and outside-workspace rejection. Native minimum/current UI verifies actual alias-buffer Apply and Undo. |
| R5 | Independent diff-review finding: after-image check runs before asynchronous writable preflight | `currentDocument` compares text before awaiting `isDocumentWritable`. A buffer change during its filesystem stat can therefore pass the old comparison, then be captured as the new proposal's version/text even though the finding refers to different bytes. This code is introduced by the PR and affects the same promised dirty-buffer safety as R4. Injecting a change during stat reproduces a missing rejection in all three ordinary/alias/subfolder fixtures, even after the URI correction. | P2: stale finding can propose an edit over newer unsaved content | Already addressed in the second pass. The after-image comparison follows asynchronous writable preflight. All three fixtures reject a preflight-time buffer change before sending any fix request. |
| R6 | [4059142157](https://github.com/narumiruna/pi-vscode/pull/36#discussion_r4059142157): sort semantic ranges numerically before truncation | The PR's new semantic adapter sorts `rangeKey` strings lexicographically before taking 50 results; attachment excerpts consume only the first 20. Independent regressions show line 100 retained before line 2 while early lines 6–9 are omitted, and also expose incorrect numeric start-column/end-line/end-column ordering. This reduces correctness of the PR's bounded semantic context, so it is in scope. | P2: wrong ordering and loss of earlier locations from bounded context; no mutation risk | Already addressed in the third pass. The comparator now subtracts start line, start column, end line and end column, retaining path precedence, the name tie-breaker and the existing deduplication key. Regressions verify all four numeric fields, provider-order independence, duplicate removal, exact earliest 50 results and first 20 excerpt headings. |
| R7 | [4059976504](https://github.com/narumiruna/pi-vscode/pull/36#discussion_r4059976504): prevent parent-symlink swaps during worktree reads | The PR introduces `readWorktreeText` for unstaged/working-tree scopes. Its parent checks precede the leaf `lstat`/`open`, so both named and opened leaf identities can describe the same foreign file. A real-filesystem fixture swaps a checked directory for an outside symlink and restores it on descriptor close; both default captures, including recapture validation, return the outside marker in provider-facing `reviewContext`. Restoring the parent immediately after open prevents publication but still reads the foreign descriptor. | P1: possible unintended disclosure of regular files outside the selected repository | Already addressed in the fourth pass. Capture retains root/ancestor identity and nanosecond metadata, verifies canonical containment and named/opened file identity before reading, and repeats path checks before retaining bytes. Opened type/link-count/size checks and nonblocking POSIX opens reject substituted or grown files. Race fixtures require no foreign descriptor reads, no foreign provider-context bytes and balanced closes. |
| R8 | [4060191776](https://github.com/narumiruna/pi-vscode/pull/36#discussion_r4060191776): bind diagnostic reads to validated workspace files | The PR's new controller checks `assertSafeFile`, then independently opens the URI in VS Code. No later validation binds its text to safe disk bytes. Against unchanged `f5ae9ca`, four fixtures substitute a leaf/parent, restore the parent before validation, or mark the newly opened foreign document dirty; all submit the outside marker. This is the same newly introduced snapshot confidentiality boundary, not a pre-existing unrelated reader. | P1: unintended disclosure of outside-workspace file contents | Already addressed in the fifth pass; see the original R8 thread for follow-up publication evidence. Clean/new documents must match descriptor-bound disk bytes before inspection; all existing dirty buffers are frozen before filesystem awaits and never reopened. Root aliases, encoding/EOL handling, bounds and stale validation remain guarded. Eight ordinary/alias substitution regressions now reject foreign context, including restored parents and newly dirty documents. |

## Validation and publication

- Against unchanged production code at `9e0a3a8`, the new/extended fixtures fail four assertions: R1 returns zero restorable files instead of one; R2 rejects the alias source as an unsafe relative path and its authenticated bridge returns `ok: false`; R3 submits zero repair requests instead of one. The other ten focused assertions/tests pass. Command: `npm run compile && npx vitest run out/test/checkpointHistory.test.js out/test/semanticContext.test.js out/test/workspaceDiagnosticsController.test.js`.
- After completing this ledger, refreshed PR state: **OPEN**, unmerged, same head. Implement fixes on its existing branch; no follow-up PR is needed unless the target merges during this work.
- Changing only the R3 collector reproduced the second failure: `Edit target is outside the selected workspace.` The adapter fix is therefore required to complete the original diagnostic-repair goal, not a separate enhancement.
- Focused validation: 15 tests across checkpoint, semantic, diagnostic-controller and multi-file adapter files pass. Full validation: **217 tests / 41 files** pass normally, with `GIT_INDEX_FILE=.git/index`, and during packaging. Whole-repository Biome passes with zero errors (380 warnings / 9 infos); production audit reports zero vulnerabilities; `git diff --check` passes.
- Native Linux VS Code **1.106.0 and 1.138.0** UI runs pass in both ordinary and directory-alias workspaces. The runner asserts VS Code retains the alias URI spelling, then exercises semantic attachments, all four reviews, Problems Ask/Fix, two-file selected-hunk Preview/Apply, one Undo, cancellation and reload. Separate Restricted Mode launches pass. These use deterministic RPC responses, not a live provider; Windows/SSH alias workflows were not rerun or newly claimed.
- The real Pi registration probe still confirms foreground-only tool registration. The inspected VSIX has 63 entries / 50 runtime modules, includes the fixes, excludes source/tests/docs/scripts/maps/development dependencies, and leaves the original ignored VSIX unchanged. SHA-256: `0603bf82cbe7641840f2566263631676004a1c3ecaaeb2a64a7255b2b191f22c`.
- Final first-pass fix review covers URI ownership, canonical locking, retained nested-symlink guards, alias retargeting before and during asynchronous Apply preflight, unchanged public commands/engine/host, lifecycle disposal and scope. No first-pass deferred item, clarification request or blocker remains. Published commit and resolution evidence are reported in the linked PR review replies.

### Second-pass validation

- Against unchanged production code at `cfb4227`, `reviewActions.test.ts` passes the ordinary-workspace control but fails the directory-alias case: two finding actions instead of Ask alone after an unsaved buffer change. The corrected native fixture independently reproduces the same extra Fix action in Linux VS Code 1.106.0. Initial test-harness iterations exposed a TypeScript assertion-narrowing error and the need to open the captured document and filter unrelated providers' code actions; these were corrected before recording the final reproduction. No production change was needed to reproduce the defect.
- R4 URI correction passes ordinary, alias and aliased-subfolder fixtures. The native fixture passes dirty-buffer rejection and actual finding Preview/Apply/Undo on Linux 1.106.0 and 1.138.0 with ordinary/alias workspaces. The fixture response includes a separate final-content newline before the replacement delimiter; an initial expected-newline mismatch was corrected in the fixture, not production parsing.
- R5 was independently reproduced during fix-diff review: all three fixtures allow a fix request when text changes during writable preflight. This is an additional in-scope finding, not feedback from a separate reviewer.
- Final focused validation: **23 tests / 6 files** pass. Full normal, Git-hook-environment and package suites: **219 tests / 41 files** pass. Whole-repository Biome passes with zero errors (374 warnings / 9 infos), `npm audit --omit=dev` reports zero vulnerabilities, and `git diff --check` passes.
- Final Linux native UI runs pass on **1.106.0 and 1.138.0**, both ordinary and aliased workspaces. Each checks dirty-buffer Ask-only availability and direct Fix rejection without sending a request, restores with native Undo, then uses native Problems Fix → Preview → Apply → Undo on the workspace document. Existing semantic, four-scope, selected-hunk batch repair, cancellation and reload checks also pass. Windows/SSH alias, live-provider and human accessibility paths are not newly claimed.
- Package inspection verifies both corrected expressions, 63 entries / 50 runtime modules and all prior exclusions. SHA-256: `800dd5d3c5dfa3c2be8faea6d936fb64705b7e359352c9b1d7eed2975a5889cb`. The original ignored VSIX is restored and checksum-verified. An initial archive-marker assertion assumed the source import name instead of TypeScript's emitted CommonJS name; the corrected check passes without changing the package.
- Complete fix-diff review covers selected-folder-relative mapping (including repository subfolders), nested-symlink/foreign-target rejection, asynchronous after-image timing, captured document/version ownership, failed-action non-submission, disposal and native Undo. Commands, stable API minimum, extension host, dependencies, trust and read-only policy remain unchanged. R1–R3 remain covered by the full suite. No second-pass deferred feedback, clarification request or blocker remains; publication and the R4 resolution evidence are recorded in the PR replies.

### Third-pass validation

- Against unchanged production code at `4de0acb`, two new semantic regressions fail while all nine existing semantic tests pass. The failures independently prove incorrect ordering of numeric range fields and a 50-result subset that includes line 100 but excludes lines 6–9. Fixtures exercise reversed provider order, duplicate locations and path precedence; the bounded fixture also checks the first 20 excerpt headings after correction.
- After completing the feedback inventory and reproduction, refreshed PR state: **OPEN**, unmerged, unchanged head. The fix stays on its existing branch.
- Final focused checks pass **24 tests / 4 files**. Normal, Git-hook-environment and package suites pass **221 tests / 41 files**. Whole-repository Biome passes with zero errors (374 warnings / 9 infos), production audit reports zero vulnerabilities, and `git diff --check` passes.
- Native Linux Extension Host API smoke runs pass on **1.106.0 and 1.138.0**, exercising real TypeScript providers, authenticated metadata lookup, cancellation, Git scopes, captured diagnostics, native diff/Apply and Undo. Numeric ordering and bound selection are proved by deterministic provider fixtures, not inferred from the small native fixture. Native UI walkthroughs, Windows/SSH and live-provider quality were not rerun for this comparator-only change; earlier evidence remains explicitly historical.
- VSIX inspection verifies all four numeric comparisons, unchanged deduplication/result-limit code, 63 entries / 50 runtime modules and all prior exclusions. SHA-256: `8c3391db976f9fa3260e51fc5561a59a03458c39eb69f63f818478d85b818f81`. The original ignored artifact is restored and checksum-verified.
- Complete fix-diff review confirms the comparator sees only validated safe-integer coordinates, retains path/name ordering and resource bounds, and adds no API, dependency, trust, lifecycle, filesystem or release change. Existing R1–R5 regressions remain covered by the full suite. No third-pass deferred feedback, clarification request or blocker remains; the published commit and R6 resolution are recorded in the PR reply.

### Fourth-pass validation

- Against unchanged production code at `dfca1f3`, four of ten new race cases fail, while the other six and five existing scope tests pass. Both scopes publish foreign bytes after a checked-parent swap; both read foreign descriptors after a swap is restored immediately following open. Additional before-open, after-open and during-read interleavings are fail-closed controls. Real files, symlinks, descriptor reads and closes are used; only scheduling boundaries are injected.
- A separate experiment swapping the parent before spawning Git's textual diff did not disclose outside content; only an overstrict test forbidding any diff invocation failed. That hypothesis is not treated as a confirmed defect, and the temporary experiment was removed rather than changing Git diff behavior without evidence.
- After the complete feedback ledger and reproduction, refreshed PR state: **OPEN**, unmerged, same head; fixes remain on its branch. The scope is the new worktree reader, not a repository-wide filesystem refactor.
- All ten swap interleavings now pass. Added controls verify nested files through canonical/aliased workspace roots, rejection before allocation/read if the opened file has grown beyond the limit, and nonblocking rejection/closure of a substituted POSIX FIFO. Focused validation: **35 tests / 4 files**. Normal, Git-hook-environment and package suites: **235 tests / 41 files**. Biome passes with zero errors (374 warnings / 9 infos), production audit reports zero vulnerabilities, and `git diff --check` passes.
- Linux VS Code **1.106.0 and 1.138.0** native API smoke tests and complete alias-workspace UI walkthroughs pass, including all review scopes, Problems Ask/Fix, Preview/Apply/Undo, cancellation and reload. Responses remain deterministic fixtures; no model call occurs.
- Actual Windows filesystem validation passes **13 compiled fixtures** under the installed VS Code executable's Node **24.18.1** mode with native Windows Git. A minimal runner registers the existing test cases and spy methods; it does not emulate filesystem behavior. Root-junction controls, six executed junction-swap variants and opened-size rejection pass; Windows itself denies the other four after-open/during-read variants, which remain fail-closed with balanced descriptor cleanup. POSIX FIFO behavior is tested only on Linux. The first Windows trial exposed a fixture-owned handle leak when its injected rename failed before returning the handle; fixture cleanup was corrected, and the final run has no GC-close warning. The owned Windows runner/workspaces are removed; unrelated earlier leftovers are untouched. This is Windows filesystem evidence, not a new Windows Extension Host UI claim. SSH/macOS/live-provider paths were not rerun.
- Package inspection verifies containment, ancestor metadata, opened-file bounds, nonblocking opens and both rechecks. The VSIX has 63 entries / 50 runtime modules with prior exclusions intact. SHA-256: `8c038d7644d57ef926f62d9c1311b3400cae4c19a595f2ffda9f158a6f4f8bec`. The original ignored artifact is restored and checksum-verified.
- Complete fix-diff review covers canonical identity binding, restored ancestors, leaf substitution/growth, all close paths, deletion compatibility, stable nested/root-alias reads, unchanged staged/branch captures, public API, engine, host and resource limits. These checks do not claim filesystem-wide atomicity against external writers. Existing R1–R6 regressions still pass. No deferred feedback, clarification request or blocker remains; publication and the R7 resolution are recorded in the PR reply.

### Fifth-pass validation

- Four initial fixtures fail against unchanged `f5ae9ca`: an actual pathname read follows a substituted leaf/parent, and foreign text reaches both the local inspection and serialized request. Restoring the parent before writable validation and marking the newly opened buffer dirty do not prevent the original disclosure. The expanded suite now rejects all eight ordinary/alias variants before inspection or submission.
- Extracted the existing R7 descriptor/ancestor checks into `workspaceFiles.ts`; worktree capture retains its UTF-8 decoding, byte limits, deletion semantics and all existing race/close tests. Diagnostic capture uses that bounded reader before opening clean/closed documents, then compares decoded/normalized text after asynchronous writable/root checks. Dirty document identity, text and version are captured for every selected file before any filesystem await; their original editor objects are used without another pathname open.
- The 29 new capture tests also verify genuine dirty-buffer/disk divergence, stale/closed buffers during preflight or confirmation, root retargeting, missing/oversized files, UTF-8/UTF-16 BOMs, mixed EOLs, configured Windows-1252, a 100,000-character multibyte file, malformed/unsupported encodings, binary text and character limits. The two existing batch workflow fixtures additionally reject changes to the second dirty buffer while the first file is being checked, then retain Preview/Apply/failure/disposal coverage.
- **54 focused tests / 5 files** pass. Normal, Git-hook-environment and package suites pass **264 tests / 42 files**. Biome passes with zero errors (389 warnings / 9 infos); production audit reports zero vulnerabilities; `git diff --check` passes. Initial validation caught a moved helper's error-label assertion and a VS Code encoding-label/`TextDecoder` alias mismatch; both were corrected, and no failing local check is concealed.
- Linux VS Code **1.106.0 and 1.138.0** native API smoke runs and full alias-workspace UI walkthroughs pass, including semantic providers, four Git scopes, finding dirty-buffer guards, Problems Fix, two-file selected-hunk Preview/Apply/Undo, cancellation and reload. No live provider call occurs; the injected race/encoding assertions are deterministic fixtures, not claims of native UI race injection.
- Actual Windows Node **24.18.1** filesystem validation passes **40 compiled fixtures**: 13 shared-reader/worktree cases and 27 diagnostic cases. The two leaf-symlink diagnostic cases are unavailable on this Windows account: `symlink` returns `EPERM` before the simulated VS Code read; both pass on Linux. Six diagnostic parent-junction/restoration/new-dirty variants execute and reject foreign context on Windows. The four previously documented worktree after-open/during-read renames remain OS-denied. The first Windows run exposed that leaf-symlink permission limit; it is not reported as an extension success. Owned helper/workspace cleanup succeeds. No new Windows Extension Host, SSH, macOS or live-provider claim is made.
- VSIX inspection verifies the shared reader and diagnostic binding, **64 entries / 51 runtime modules**, packaged bridge/gates and all prior source/test/docs/script/map/dependency exclusions. SHA-256: `1b9ab1087c4cf008e7279c0dce461c786de701ad09103ae794bacbbf8c13510b`. The original ignored VSIX is restored and checksum-verified.
- Complete fix review covers source provenance, before-await dirty ownership across files, canonical/editor alias separation, immutable captured strings, descriptor cleanup, bounds, decoding fail-closed behavior and retained R1–R7 regressions. No command, engine, host, trust, dependency or release contract changes. Unverifiable encodings are explicitly rejected and documented; guards do not promise filesystem-wide atomicity. A patch Changeset records this fix. No deferred feedback or clarification remains. See the original R8 review thread for the signed follow-up commit/PR and publication outcome.
