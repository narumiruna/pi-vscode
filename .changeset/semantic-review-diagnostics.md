---
"pi-coding-agent-vscode": minor
---

Add trusted language-provider context attachments and the foreground-only `vscode_code_context` bridge tool. Review staged, unstaged, working-tree and branch changes as immutable snapshots with captured findings in Problems and guarded Ask/Fix actions. Repair selected workspace diagnostics through bounded multi-file proposals with hunk selection, native diffs, one explicit workspace edit and observational diagnostic feedback. Existing staged-review commands and VS Code 1.106 compatibility are preserved. Semantic lookup retains attributable checkpoint coverage; semantic context and diagnostic repair support workspace-root aliases without allowing nested symlink targets or retargeted repair roots. Finding fixes preserve aliased editor buffers and reject content changes during writable preflight. Semantic results use numeric source-coordinate order before result and excerpt limits.
